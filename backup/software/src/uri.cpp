
#include <stdint.h>

// {add_device_to_server, [device id]} // add device to server
// {rem_device_from_server, [device id]} // remove device from server
// {get_active_devices, null} // return object with {show_ctive_devices, {number, [device id, device id, device id]}}
// {play_movie, movie} // return object with {show_ctive_devices, {number, [device id, device id, device id]}}

